#include "main.h"
#include <string.h>

/**
 * _strlen - takes a pointer of type char
 * @s: pointer of type char
 * Return: the length of string that pointer pointed on
 */

int _strlen(char *s)
{
	return (strlen(s));
}
