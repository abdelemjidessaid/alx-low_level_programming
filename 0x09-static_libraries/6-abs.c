#include "main.h"

/**
 * _abs - returns the absolute value of numbers
 * @n: The input number
 * Return: return the absolute value of input number
 */
int _abs(int n)
{
	if (n < 0)
		n *= -1;
	return (n);
}
