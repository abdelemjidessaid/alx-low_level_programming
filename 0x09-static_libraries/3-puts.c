#include "main.h"
#include <stdio.h>

/**
 * _puts - prints the value of a given string as a pointer
 * of type char
 * @str: pointer pointed on variable of type charesquence
 * Return: void
 */

void _puts(char *str)
{
	puts(str);
}
