#include <stdio.h>

/**
 * main - Entery point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char quote[] = "\"Programming is like building a multilingual puzzle\n";
puts(quote);
return (0);
}
