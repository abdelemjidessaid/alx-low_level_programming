#include <stdio.h>

/**
 * main - Entery point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char quote = '"';
puts("%cProgramming is like building a multilingual puzzle\n", quote);
return (0);
}
